quafu.dtype.is_same_precision
===================================

.. py:function:: quafu.dtype.is_same_precision(dtype1, dtype2)

    判断两个类型的精度是否相同。

    参数：
        - **dtype1** (quafu.dtype) - quafu 支持的类型。
        - **dtype2** (quafu.dtype) - quafu 支持的类型。
