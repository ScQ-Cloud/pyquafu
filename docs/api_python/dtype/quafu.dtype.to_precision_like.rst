quafu.dtype.to_precision_like
===================================

.. py:function:: quafu.dtype.to_precision_like(dtype_src, dtype_des)

    将一个类型转为跟目标类型精度相同的类型。

    参数：
        - **dtype_src** (quafu.dtype) - quafu 支持的类型。
        - **dtype_des** (quafu.dtype) - quafu 支持的类型。
